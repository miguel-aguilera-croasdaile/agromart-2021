<% begin %>
  <% if current_page?(controller: "listings", action: "show") %>
    <div class="container">
      <div class="row">
        <div class="breadcrumbs">
          <%= link_to "Home", root_path %> > <%= link_to "Listings", listings_path %> > <%= @listing.name %>
        </div>
      </div>
    </div>
  <% end %>
  <% rescue %>
<% end %>
